import React from 'react'

const OrderConfirm = () => {
  //Stripe occurs, payment received, then...
  //When this page is reached, empty out the users cart by marking every item to "completed"
  return <div>Thank you for your order! </div>
}

export default OrderConfirm
